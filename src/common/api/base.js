/**
 * 接口域名的管理
 */
const domain = "";
const base = {
    photoCategory: `${domain}/api/evi-category`,
    nvr: `${domain}/warehouse/nvr`,
    cameras: `${domain}/warehouse/cameras`,
}

export default base;